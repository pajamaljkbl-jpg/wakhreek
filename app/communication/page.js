'use client'
import Link from 'next/link'
import {useCallback,useEffect,useMemo,useRef,useState} from 'react'
import {useRouter} from 'next/navigation'
import {supabase} from '../../lib/supabase'
import styles from './communication.module.css'

export default function CommunicationPage(){
 const router=useRouter(); const [me,setMe]=useState(null),[people,setPeople]=useState([]),[friendIds,setFriendIds]=useState(new Set()),[incomingRequests,setIncomingRequests]=useState([]),[outgoingRequests,setOutgoingRequests]=useState([]),[conversationMeta,setConversationMeta]=useState({}),[activePerson,setActivePerson]=useState(null),[conversationId,setConversationId]=useState(null),[messages,setMessages]=useState([]),[text,setText]=useState(''),[filter,setFilter]=useState('toutes'),[status,setStatus]=useState(''),[recording,setRecording]=useState(false),[uploadingAudio,setUploadingAudio]=useState(false),[friendBox,setFriendBox]=useState(false),[phoneSearch,setPhoneSearch]=useState(''),[phoneResults,setPhoneResults]=useState([]),[phoneBusy,setPhoneBusy]=useState(false)
 const messagesEndRef=useRef(null),mediaRecorderRef=useRef(null),mediaStreamRef=useRef(null),audioChunksRef=useRef([])
 const withSignedMedia=useCallback(async m=>{if(m?.message_type!=='audio'||!m?.storage_path)return m;const{data}=await supabase.storage.from('communication-media').createSignedUrl(m.storage_path,3600);return data?.signedUrl?{...m,media_url:data.signedUrl}:m},[])
 const refreshSidebar=useCallback(async user=>{if(!user)return;const[{data:foundPeople,error:pe},{data:fr,error:fe},{data:req,error:re},{data:members,error:me2}]=await Promise.all([supabase.rpc('find_people',{search_text:''}),supabase.from('friends').select('friend_id'),supabase.from('friend_requests').select('id,sender_id,receiver_id,status,created_at').eq('status','pending').order('created_at',{ascending:false}),supabase.from('conversation_members').select('conversation_id,user_id')]);if(pe||fe||re||me2){setStatus((pe||fe||re||me2).message);return}setPeople(foundPeople||[]);setFriendIds(new Set((fr||[]).map(x=>x.friend_id)));setIncomingRequests((req||[]).filter(x=>x.receiver_id===user.id));setOutgoingRequests((req||[]).filter(x=>x.sender_id===user.id));const map=new Map();(members||[]).forEach(x=>{if(x.user_id!==user.id)map.set(x.conversation_id,x.user_id)});const ids=[...map.keys()];if(!ids.length){setConversationMeta({});return}const{data:latest}=await supabase.from('messages').select('conversation_id,body,message_type,created_at').in('conversation_id',ids).order('created_at',{ascending:false});const lm=new Map();(latest||[]).forEach(x=>{if(!lm.has(x.conversation_id))lm.set(x.conversation_id,x)});const meta={};map.forEach((uid,id)=>meta[uid]={id,latest:lm.get(id)||null});setConversationMeta(meta)},[])
 useEffect(()=>{supabase.auth.getUser().then(({data,error})=>{if(error||!data?.user){router.replace('/');return}setMe(data.user);refreshSidebar(data.user)})},[router,refreshSidebar])
 const loadMessages=useCallback(async id=>{const{data,error}=await supabase.from('messages').select('*').eq('conversation_id',id).order('created_at',{ascending:true});if(error){setStatus(error.message);return}setMessages(await Promise.all((data||[]).map(withSignedMedia)))},[withSignedMedia])
 useEffect(()=>{if(!conversationId)return;loadMessages(conversationId);const ch=supabase.channel(`messages:${conversationId}`).on('postgres_changes',{event:'INSERT',schema:'public',table:'messages',filter:`conversation_id=eq.${conversationId}`},async p=>{const m=await withSignedMedia(p.new);setMessages(c=>c.some(x=>x.id===m.id)?c:[...c,m]);if(me)refreshSidebar(me)}).subscribe();return()=>supabase.removeChannel(ch)},[conversationId,loadMessages,me,refreshSidebar,withSignedMedia])
 useEffect(()=>messagesEndRef.current?.scrollIntoView({behavior:'smooth'}),[messages]);useEffect(()=>()=>mediaStreamRef.current?.getTracks().forEach(t=>t.stop()),[])
 const peopleById=useMemo(()=>new Map(people.map(p=>[p.id,p])),[people]),incomingBySender=useMemo(()=>new Map(incomingRequests.map(r=>[r.sender_id,r])),[incomingRequests]),outgoingByReceiver=useMemo(()=>new Map(outgoingRequests.map(r=>[r.receiver_id,r])),[outgoingRequests])
 const visiblePeople=useMemo(()=>{if(filter==='demandes'||filter==='groupes')return[];let list=people.filter(p=>friendIds.has(p.id)||conversationMeta[p.id]);if(filter==='amis')list=list.filter(p=>friendIds.has(p.id));return [...list].sort((a,b)=>(conversationMeta[b.id]?.latest?.created_at||'').localeCompare(conversationMeta[a.id]?.latest?.created_at||''))},[people,friendIds,conversationMeta,filter])
 async function searchPhone(e){e?.preventDefault();const q=phoneSearch.trim();if(!q)return;setPhoneBusy(true);setStatus('');const{data,error}=await supabase.rpc('find_people_by_phone',{search_text:q});setPhoneBusy(false);if(error){setStatus(error.message);return}setPhoneResults(data||[])}
 async function sendFriendRequest(person){if(!me||friendIds.has(person.id)||outgoingByReceiver.has(person.id))return;const{error}=await supabase.from('friend_requests').insert({sender_id:me.id,receiver_id:person.id});if(error){setStatus(`Invitation: ${error.message}`);return}setStatus('Invitation envoyée ✅');await refreshSidebar(me);await searchPhone()}
 async function removeFriend(person){if(!me||!friendIds.has(person.id))return;if(!window.confirm(`Supprimer ${person.display_name||person.phone||'cet ami'} de vos amis ?`))return;const{error}=await supabase.rpc('remove_friend',{other_user:person.id});if(error){setStatus(error.message);return}setStatus('Ami supprimé.');await refreshSidebar(me);setPhoneResults(r=>r.map(x=>x.id===person.id?{...x}:x))}
 async function respondFriendRequest(id,accept){const{error}=await supabase.rpc('respond_friend_request',{request_id:id,accept_request:accept});if(error){setStatus(error.message);return}setStatus(accept?'Ami ajouté ✅':'Invitation refusée.');await refreshSidebar(me);if(accept)setFilter('amis')}
 async function openConversation(person){const existing=conversationMeta[person.id]?.id;if(!existing&&!friendIds.has(person.id)){setStatus('Ajoutez cette personne comme ami avant de démarrer une discussion.');return}let id=existing;if(!id){const{data,error}=await supabase.rpc('start_direct_conversation',{other_user:person.id});if(error){setStatus(error.message);return}id=data}setActivePerson(person);setConversationId(id)}
 async function sendMessage(e){e.preventDefault();const body=text.trim();if(!body||!conversationId||!me)return;setText('');const{error}=await supabase.from('messages').insert({conversation_id:conversationId,sender_id:me.id,body,message_type:'text'});if(error){setText(body);setStatus(error.message)}}
 async function startVoiceRecording(){if(!activePerson||!conversationId||!me||recording||uploadingAudio)return;if(!navigator.mediaDevices?.getUserMedia||typeof MediaRecorder==='undefined'){setStatus("L'enregistrement audio n'est pas supporté.");return}try{const stream=await navigator.mediaDevices.getUserMedia({audio:true});mediaStreamRef.current=stream;audioChunksRef.current=[];const types=['audio/webm;codecs=opus','audio/webm','audio/ogg;codecs=opus','audio/ogg'];const mt=types.find(t=>MediaRecorder.isTypeSupported?.(t))||'';const rec=mt?new MediaRecorder(stream,{mimeType:mt}):new MediaRecorder(stream);mediaRecorderRef.current=rec;rec.ondataavailable=e=>{if(e.data?.size)audioChunksRef.current.push(e.data)};rec.onstop=async()=>{setRecording(false);stream.getTracks().forEach(t=>t.stop());const actual=rec.mimeType||mt||'audio/webm',base=actual.split(';')[0],ext=base.includes('ogg')?'ogg':base.includes('mpeg')?'mp3':'webm',blob=new Blob(audioChunksRef.current,{type:actual});audioChunksRef.current=[];if(!blob.size)return;setUploadingAudio(true);const path=`${conversationId}/${me.id}/${Date.now()}.${ext}`;const{error:ue}=await supabase.storage.from('communication-media').upload(path,blob,{contentType:base,upsert:false});if(ue){setUploadingAudio(false);setStatus(ue.message);return}const{error}=await supabase.from('messages').insert({conversation_id:conversationId,sender_id:me.id,body:'Message vocal',message_type:'audio',storage_path:path,media_url:null});setUploadingAudio(false);if(error)setStatus(error.message)};rec.start();setRecording(true)}catch(e){setRecording(false);setStatus(e?.name==='NotAllowedError'?'Autorisez le microphone.':e.message)}}
 function stopVoiceRecording(){const r=mediaRecorderRef.current;if(r&&r.state!=='inactive')r.stop()} async function signOut(){await supabase.auth.signOut();router.replace('/')} function fmt(v){return v?new Date(v).toLocaleTimeString('fr-FR',{hour:'2-digit',minute:'2-digit'}):''} function initials(p){return(p?.display_name||p?.email||'WR').slice(0,2).toUpperCase()} function latest(p){const m=conversationMeta[p.id]?.latest;if(!m)return friendIds.has(p.id)?'Ami WakhReek':'Discussion';if(m.message_type==='audio')return'🎙 Message vocal';return m.body||'Message'}
 return <div className={styles.page}>
  <header className={styles.topbar}><div className={styles.brand}><img src="/wakhreek-logo.svg" alt="WakhReek" className={styles.logo}/><div><strong>WakhReek</strong><span>- Onley Took</span></div></div><div className={styles.topActions}><button className={styles.iconButton} onClick={()=>me&&refreshSidebar(me)}>↻</button><div className={styles.meBlock}><div className={styles.avatarSmall}>{initials({email:me?.email})}</div><div><strong>{me?.email||'WakhReek'}</strong><span>En ligne</span></div></div><button className={styles.logout} onClick={signOut}>↪ Déconnexion</button></div></header>
  <nav className={styles.mainNav}><Link className={styles.navActive} href="/communication">💬 Communication</Link><Link href="/market">🛍 Market</Link><Link href="/tv">📺 WakhReek TV</Link></nav>
  <div className={`${styles.shell} ${activePerson?styles.hasActive:''}`}><aside className={styles.sidebar}>
   <div className={styles.searchWrap}><button type="button" className={styles.addFriendOpen} onClick={()=>{setFriendBox(v=>!v);setPhoneResults([]);setPhoneSearch('')}}>＋</button><span>⌕</span><input readOnly placeholder="Rechercher ou démarrer une discussion"/></div>
   {friendBox&&<div className={styles.friendBox}><div className={styles.friendBoxHead}><strong>Ajouter un ami</strong><button onClick={()=>setFriendBox(false)}>×</button></div><form onSubmit={searchPhone}><input type="tel" value={phoneSearch} onChange={e=>setPhoneSearch(e.target.value)} placeholder="Numéro de téléphone" autoFocus/><button type="submit" disabled={phoneBusy}>{phoneBusy?'…':'⌕'}</button></form>{phoneResults.map(p=>{const isFriend=friendIds.has(p.id),out=outgoingByReceiver.get(p.id),inc=incomingBySender.get(p.id);return <div className={styles.phoneResult} key={p.id}><div className={styles.contactAvatar}>{initials(p)}</div><div><strong>{p.display_name||p.phone||'Utilisateur WakhReek'}</strong><span>{p.phone}</span></div>{isFriend?<button className={styles.removeFriend} onClick={()=>removeFriend(p)}>Supprimer</button>:out?<span className={styles.pendingBadge}>En attente</span>:inc?<button className={styles.friendAction} onClick={()=>setFilter('demandes')}>Demande</button>:<button className={styles.friendAction} onClick={()=>sendFriendRequest(p)}>Ajouter</button>}</div>})}{phoneSearch&&phoneResults.length===0&&!phoneBusy&&<p className={styles.friendHint}>Tapez le numéro puis appuyez sur recherche.</p>}</div>}
   <div className={styles.filters}>{[['toutes','Toutes'],['amis','Amis'],['demandes',incomingRequests.length?`Demandes (${incomingRequests.length})`:'Demandes'],['groupes','Groupes']].map(([k,l])=><button key={k} className={filter===k?styles.filterActive:''} onClick={()=>setFilter(k)}>{l}</button>)}</div>
   <div className={styles.contactList}>{filter==='demandes'?(incomingRequests.length?incomingRequests.map(r=>{const p=peopleById.get(r.sender_id);return p?<div className={styles.requestRow} key={r.id}><div className={styles.contactAvatar}>{initials(p)}</div><div className={styles.requestCopy}><strong>{p.display_name||p.email}</strong><span>يريد إضافتك كصديق</span></div><div className={styles.requestActions}><button className={styles.acceptButton} onClick={()=>respondFriendRequest(r.id,true)}>✓</button><button className={styles.rejectButton} onClick={()=>respondFriendRequest(r.id,false)}>×</button></div></div>:null}):<p className={styles.empty}>Aucune demande d&apos;ami</p>):visiblePeople.map(p=><div className={styles.contactRow} key={p.id}><button className={styles.personMain} onClick={()=>openConversation(p)}><div className={styles.contactAvatar}>{initials(p)}<i/></div><div className={styles.contactCopy}><div><strong>{p.display_name||p.email}</strong><time>{fmt(conversationMeta[p.id]?.latest?.created_at)}</time></div><p>{latest(p)}</p></div></button></div>)}{filter!=='demandes'&&!visiblePeople.length&&<p className={styles.empty}>Aucune conversation</p>}</div><div className={styles.encryption}>🔒 Vos messages personnels sont <strong>protégés</strong></div>
  </aside><main className={styles.chat}><header className={styles.chatHeader}><button className={styles.mobileBack} onClick={()=>{setActivePerson(null);setConversationId(null);setMessages([])}}>←</button><div className={styles.chatIdentity}><div className={styles.avatarMedium}>{initials(activePerson)}</div><div><strong>{activePerson?.display_name||'WakhReek Communication'}</strong><span>{activePerson?'En ligne':'Choisissez un contact'}</span></div></div><div className={styles.chatActions}><button>⌕</button><button disabled>☎</button><button disabled>▣</button><button>⋮</button></div></header><section className={styles.messages}>{!activePerson&&<div className={styles.welcome}>Choisissez une discussion pour commencer.</div>}{messages.map(m=>{const mine=m.sender_id===me?.id;return <div className={`${styles.messageRow} ${mine?styles.mine:''}`} key={m.id}><div className={`${styles.bubble} ${mine?styles.bubbleMine:''}`}>{m.message_type==='audio'&&m.media_url?<audio controls src={m.media_url} className={styles.audio}/>:<p>{m.body}</p>}<time>{fmt(m.created_at)} {mine?'✓✓':''}</time></div></div>})}{status&&<div className={styles.status}>{status}</div>}<div ref={messagesEndRef}/></section><form className={styles.composer} onSubmit={sendMessage}><button disabled>☺</button><button disabled>⌕</button><input value={text} onChange={e=>setText(e.target.value)} placeholder={recording?'Enregistrement…':activePerson?'Message':'Choisissez une conversation'} disabled={!activePerson||recording||uploadingAudio}/><button disabled>📎</button><button disabled>📷</button>{text.trim()?<button type="submit" className={styles.send}>➤</button>:<button type="button" className={`${styles.send} ${recording?styles.recording:''}`} onClick={recording?stopVoiceRecording:startVoiceRecording} disabled={!activePerson||uploadingAudio}>{recording?'■':'🎙'}</button>}</form></main></div>
 </div>
}
