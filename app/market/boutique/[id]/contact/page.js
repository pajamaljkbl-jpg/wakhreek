'use client'
import {useEffect,useRef,useState} from 'react'
import {useParams,useRouter} from 'next/navigation'
import AppShell from '../../../../../components/AppShell'
import {supabase} from '../../../../../lib/supabase'
const RTC={iceServers:[{urls:'stun:stun.l.google.com:19302'},{urls:'stun:stun1.l.google.com:19302'}]}
export default function BoutiqueContactPage(){
 const {id}=useParams(),router=useRouter(),fileRef=useRef(null),pcRef=useRef(null),localRef=useRef(null),remoteRef=useRef(null),localVideo=useRef(null),remoteVideo=useRef(null),pollRef=useRef(null)
 const [boutique,setBoutique]=useState(null),[user,setUser]=useState(null),[messages,setMessages]=useState([]),[text,setText]=useState(''),[notice,setNotice]=useState(''),[loading,setLoading]=useState(true),[mediaType,setMediaType]=useState('image'),[busy,setBusy]=useState(false),[peerId,setPeerId]=useState(null),[call,setCall]=useState(null),[incoming,setIncoming]=useState(null)
 async function load(){setLoading(true);const{data:auth}=await supabase.auth.getUser(),u=auth?.user||null;setUser(u);const{data:b}=await supabase.from('boutiques').select('id,user_id,name,owner_display_name,owner_job_title,logo_url,is_live').eq('id',id).eq('is_live',true).maybeSingle();setBoutique(b||null);if(b&&u){let other=b.user_id;if(u.id===b.user_id){const{data:last}=await supabase.from('market_boutique_messages').select('sender_id,recipient_id').eq('boutique_id',b.id).or(`sender_id.eq.${u.id},recipient_id.eq.${u.id}`).order('created_at',{ascending:false}).limit(1).maybeSingle();other=last?(last.sender_id===u.id?last.recipient_id:last.sender_id):null}setPeerId(other);const{data:m}=await supabase.from('market_boutique_messages').select('*').eq('boutique_id',b.id).or(`sender_id.eq.${u.id},recipient_id.eq.${u.id}`).order('created_at',{ascending:true});setMessages(m||[]);const{data:inc}=await supabase.from('market_boutique_calls').select('*').eq('boutique_id',b.id).eq('callee_id',u.id).eq('status','ringing').order('created_at',{ascending:false}).limit(1).maybeSingle();setIncoming(inc||null)}setLoading(false)}
 useEffect(()=>{load()},[id]);useEffect(()=>{if(!user||!boutique)return;pollRef.current=setInterval(async()=>{if(call){const{data:c}=await supabase.from('market_boutique_calls').select('*').eq('id',call.id).maybeSingle();if(c)await syncCall(c)}else{const{data:c}=await supabase.from('market_boutique_calls').select('*').eq('boutique_id',boutique.id).eq('callee_id',user.id).eq('status','ringing').order('created_at',{ascending:false}).limit(1).maybeSingle();setIncoming(c||null)}},1500);return()=>clearInterval(pollRef.current)},[user?.id,boutique?.id,call?.id])
 useEffect(()=>()=>cleanup(),[])
 async function sendMessage(){if(!user||!peerId||!text.trim())return setNotice(!peerId?'Aucun interlocuteur sélectionné.':'');const{error}=await supabase.from('market_boutique_messages').insert({boutique_id:boutique.id,sender_id:user.id,recipient_id:peerId,message_type:'text',body:text.trim()});if(error)return setNotice(error.message);setText('');await load()}
 function chooseMedia(type){setMediaType(type);if(fileRef.current){fileRef.current.accept=type==='image'?'image/*':type==='audio'?'audio/*':'video/*';fileRef.current.click()}}
 async function sendMedia(file){if(!file||!user||!peerId)return;const ok=mediaType==='image'?file.type.startsWith('image/'):mediaType==='audio'?file.type.startsWith('audio/'):file.type.startsWith('video/');if(!ok)return setNotice('Type de fichier invalide.');const max=mediaType==='image'?8:mediaType==='audio'?20:50;if(file.size>max*1048576)return setNotice(`Fichier supérieur à ${max} Mo.`);setBusy(true);try{const ext=(file.name.split('.').pop()||'bin').replace(/[^a-z0-9]/gi,''),path=`${user.id}/${boutique.id}/chat/${Date.now()}-${Math.random().toString(36).slice(2,8)}.${ext}`;const{error:up}=await supabase.storage.from('wakhreek-images').upload(path,file,{contentType:file.type});if(up)throw up;const url=supabase.storage.from('wakhreek-images').getPublicUrl(path).data.publicUrl;const{error}=await supabase.from('market_boutique_messages').insert({boutique_id:boutique.id,sender_id:user.id,recipient_id:peerId,message_type:mediaType,media_url:url});if(error)throw error;await load()}catch(e){setNotice(e.message)}finally{setBusy(false)}}
 function cleanup(){if(pcRef.current){pcRef.current.close();pcRef.current=null}if(localRef.current){localRef.current.getTracks().forEach(t=>t.stop());localRef.current=null}remoteRef.current=null;if(localVideo.current)localVideo.current.srcObject=null;if(remoteVideo.current)remoteVideo.current.srcObject=null}
 async function makePC(row,isCaller){const pc=new RTCPeerConnection(RTC);pcRef.current=pc;const stream=await navigator.mediaDevices.getUserMedia({audio:true,video:row.call_type==='video'});localRef.current=stream;if(localVideo.current)localVideo.current.srcObject=stream;stream.getTracks().forEach(t=>pc.addTrack(t,stream));pc.ontrack=e=>{remoteRef.current=e.streams[0];if(remoteVideo.current)remoteVideo.current.srcObject=e.streams[0]};pc.onicecandidate=async e=>{if(!e.candidate)return;const col=isCaller?'caller_ice':'callee_ice';const{data:f}=await supabase.from('market_boutique_calls').select(col).eq('id',row.id).single();const arr=Array.isArray(f?.[col])?f[col]:[];await supabase.from('market_boutique_calls').update({[col]:[...arr,e.candidate.toJSON()]}).eq('id',row.id)};return pc}
 async function startCall(type){if(!user||!peerId)return setNotice('Aucun interlocuteur sélectionné.');try{const base={boutique_id:boutique.id,caller_id:user.id,callee_id:peerId,call_type:type,status:'ringing'};const{data:row,error}=await supabase.from('market_boutique_calls').insert(base).select('*').single();if(error)throw error;const pc=await makePC(row,true),offer=await pc.createOffer();await pc.setLocalDescription(offer);const{data:updated,error:uerr}=await supabase.from('market_boutique_calls').update({offer:offer.toJSON()}).eq('id',row.id).select('*').single();if(uerr)throw uerr;setCall(updated);setNotice('Appel en cours…')}catch(e){cleanup();setNotice(e?.name==='NotAllowedError'?'Autorisez le micro/caméra.':e.message)}}
 async function acceptCall(){try{const row=incoming,pc=await makePC(row,false);await pc.setRemoteDescription(new RTCSessionDescription(row.offer));for(const c of row.caller_ice||[])try{await pc.addIceCandidate(c)}catch{}const answer=await pc.createAnswer();await pc.setLocalDescription(answer);const{data:updated,error}=await supabase.from('market_boutique_calls').update({answer:answer.toJSON(),status:'accepted',answered_at:new Date().toISOString()}).eq('id',row.id).select('*').single();if(error)throw error;setIncoming(null);setCall(updated);setNotice('Appel connecté.')}catch(e){cleanup();setNotice(e?.name==='NotAllowedError'?'Autorisez le micro/caméra.':e.message)}}
 async function syncCall(c){if(c.status==='declined'||c.status==='ended'){cleanup();setCall(null);setNotice(c.status==='declined'?'Appel refusé.':'Appel terminé.');return}const pc=pcRef.current;if(!pc)return;if(user.id===c.caller_id&&c.answer&&pc.remoteDescription==null){await pc.setRemoteDescription(new RTCSessionDescription(c.answer));for(const x of c.callee_ice||[])try{await pc.addIceCandidate(x)}catch{}setNotice('Appel connecté.')}else if(user.id===c.callee_id){for(const x of c.caller_ice||[])try{await pc.addIceCandidate(x)}catch{}}setCall(c)}
 async function decline(){if(!incoming)return;await supabase.from('market_boutique_calls').update({status:'declined',ended_at:new Date().toISOString()}).eq('id',incoming.id);setIncoming(null)}
 async function endCall(){if(call)await supabase.from('market_boutique_calls').update({status:'ended',ended_at:new Date().toISOString()}).eq('id',call.id);cleanup();setCall(null);setNotice('Appel terminé.')}
 if(loading)return <AppShell><main className="page">Chargement...</main></AppShell>;if(!boutique)return <AppShell><main className="page">Boutique indisponible</main></AppShell>;const owner=boutique.owner_display_name||boutique.name
 return <AppShell><main className="page"><div className="window"><header><button onClick={()=>router.push(`/market/boutique/${boutique.id}`)}>←</button><div className="avatar">{boutique.logo_url?<img src={boutique.logo_url} alt={owner}/>:'👤'}</div><div><b>{owner}</b><small>{boutique.owner_job_title||boutique.name}</small></div></header>{incoming&&<div className="incoming"><b>{incoming.call_type==='video'?'📹 Appel vidéo entrant':'📞 Appel audio entrant'}</b><div><button onClick={acceptCall}>Accepter</button><button className="red" onClick={decline}>Refuser</button></div></div>} {call&&<div className="callbox"><div className="videos"><video ref={remoteVideo} autoPlay playsInline/><video ref={localVideo} autoPlay muted playsInline/></div><button className="red" onClick={endCall}>☎ Terminer</button></div>}<div className="tools"><button>💬<span>Message</span></button><button onClick={()=>chooseMedia('image')}>📷<span>Photo</span></button><button onClick={()=>chooseMedia('audio')}>🎙️<span>Audio</span></button><button onClick={()=>chooseMedia('video')}>🎥<span>Vidéo</span></button><button className="green" onClick={()=>startCall('audio')} disabled={!!call}>📞<span>Appel</span></button><button className="green" onClick={()=>startCall('video')} disabled={!!call}>📹<span>Visio</span></button><input ref={fileRef} hidden type="file" onChange={e=>sendMedia(e.target.files?.[0])}/></div><div className="thread">{messages.map(m=><div key={m.id} className={`bubble ${m.sender_id===user?.id?'mine':'theirs'}`}>{m.message_type==='text'&&<p>{m.body}</p>}{m.message_type==='image'&&<img className="media" src={m.media_url} alt="Photo"/>}{m.message_type==='audio'&&<audio controls src={m.media_url}/>} {m.message_type==='video'&&<video className="media" controls src={m.media_url}/>}<small>{new Date(m.created_at).toLocaleTimeString('fr-FR',{hour:'2-digit',minute:'2-digit'})}</small></div>)}</div><div className="composer"><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Écrire un message..."/><button onClick={sendMessage}>Envoyer</button></div>{busy&&<p className="notice">Envoi…</p>}{notice&&<p className="notice">{notice}</p>}</div></main><style jsx>{`.page{min-height:calc(100vh - 140px);padding:20px;background:#eef3f9;display:flex;justify-content:center;color:#172033}.window{width:min(100%,650px);height:calc(100vh - 180px);min-height:580px;background:#fff;border-radius:20px;overflow:hidden;display:flex;flex-direction:column}header{background:#0875e8;color:#fff;padding:12px;display:flex;align-items:center;gap:10px}header button{background:none;border:0;color:#fff;font-size:25px}header div:last-child{display:grid}header small{opacity:.85}.avatar{width:46px;height:46px;border-radius:50%;overflow:hidden;background:#fff;display:grid;place-items:center}.avatar img{width:100%;height:100%;object-fit:cover}.tools{display:grid;grid-template-columns:repeat(6,1fr);gap:4px;padding:8px;background:#f8fafc}.tools button{border:0;border-radius:9px;padding:7px 2px;background:#dcebff;color:#075dcc;display:grid;place-items:center;cursor:pointer}.tools span{font-size:9px}.tools .green{background:#e7f7ed;color:#176b3a}.incoming{padding:12px;background:#fff4d8;display:flex;justify-content:space-between;align-items:center}.incoming button,.callbox button{border:0;border-radius:8px;padding:8px 12px;background:#1b8b4b;color:#fff;margin-left:5px}.red{background:#d92d20!important;color:#fff!important}.callbox{background:#101828;padding:8px;text-align:center}.videos{position:relative;min-height:180px}.videos video:first-child{width:100%;max-height:280px;background:#000}.videos video:last-child{position:absolute;width:110px;height:90px;object-fit:cover;right:8px;bottom:8px;background:#222}.thread{flex:1;padding:12px;overflow:auto;background:#f5f7fa;display:flex;flex-direction:column;gap:8px}.bubble{max-width:78%;padding:9px;border-radius:12px}.mine{align-self:flex-end;background:#dcebff}.theirs{align-self:flex-start;background:#fff}.bubble p{margin:0}.bubble small{display:block;font-size:9px;color:#667085}.media{max-width:100%;max-height:260px;border-radius:8px}audio{max-width:100%}.composer{display:grid;grid-template-columns:1fr auto;gap:7px;padding:9px}.composer textarea{border:1px solid #ccd5e2;border-radius:10px;padding:9px;resize:none}.composer button{border:0;border-radius:9px;background:#0875e8;color:#fff;font-weight:700}.notice{margin:0;padding:7px 10px;background:#fff4e5;color:#8a5200;font-size:12px}@media(max-width:700px){.page{padding:0}.window{width:100%;height:calc(100vh - 130px);min-height:0;border-radius:0}}`}</style></AppShell>}
